# Port uvicorn binds inside the container (compose/Coolify may override)
PORT=8000

# Postgres. The database must exist; tables are created automatically.
DATABASE_URL=postgresql://postgres:CHANGE_ME@192.168.0.5:5432/freeaudiobook

# Signs the session cookie (anonymous identity + logins). Long random string.
SECRET_KEY=CHANGE_ME
